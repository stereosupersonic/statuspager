server {
    listen 80;
    server_name _;

    # exact match for "/" — no URI suffix is ever appended
    location = / {
        default_type text/plain;
        alias /data/archimedes.txt;  # points directly at the file
    }

    # everything else → 404
    location / {
        return 404;
    }
}
